---
status: closed
---

# fix: 同梱経路では Phase E の親伝播を起動する辺がどこにも無い

## 背景

`dev-workflow:in-repo-issue` の Phase E (子が全部 closed になったら親の close を提案する) を
起動するのは Phase D.5 である。D.5 は Phase D の最後の手順なので、Phase C が「未チェック 0」と
判定して Phase D へ進んだときにだけ通る。

クローズを feature PR へ同梱する経路には、この辺が無い。

- 同梱節が feature ブランチで行えと書いているのは D.1〜D.4 で、D.5 は範囲の外にある
- マージ後に Phase C が走っても、対象は既に `closed/` 配下にあるので C.3 の「既に closed」
  分岐で no-op になる。Phase D へ進まないので D.5 にも到達しない

同梱節自身は「親 Issue の Phase E 伝播 close も、親を閉じる PR に同梱するか、直 push が
許されない環境では別 PR で行う」と書いている。これは伝播をどこで実行するかを決めているだけで、
伝播が要ることを誰が検出するかは決めていない。検出は D.5 が持つ。

`scripts/check-issue-closure.py` が入ったことで、このリポジトリではクローズの同梱が既定に
なった。つまり Phase E は既定の経路から起動しなくなる。

## 発火しないことに気づけない理由

親子 Issue が現在 0 件なので、起動しなくても症状が出ない。

新検査も代替にならない。不変条件 A は active な Issue の箱が全て `[x]` であることを見るが、
A.5 が定める親の `## タスク` は子 Issue へのリンクを並べた箱で、Phase E は「親のチェック状態は
判定に使わない」と明記している。子が closed になっても親の箱は未チェックのまま残るので、
不変条件 A は発火しない。

## 実測 (2026-08-29)

| 観測 | 結果 |
| --- | --- |
| frontmatter に `parent` / `children` を持つ Issue | 0 件 |
| 同梱節が feature ブランチへ含めろと書く範囲 | D.1〜D.4 |
| gate SKILL.md の Phase 2 / Phase 3 が同梱時に適用しろと書く範囲 | 同梱節へ委譲。範囲を持たない |

gate は範囲を持たず同梱節へ委譲するので、射程の canonical は同梱節 1 箇所だけにある。
そこが D.1〜D.4 に限っている以上、canonical をそのまま辿ると D.5 が落ちる。手当ては
同梱節の側へ入れれば足り、gate は自動で追随する。

## ISSUE-38 との違い

症状 (Phase E の親伝播が静かに発火しない) は同じだが、機序が違う。

| | 起動 | 抽出 |
| --- | --- | --- |
| ISSUE-38 | 起動する | frontmatter のキー名が規約と違うので 0 件を返す |
| 本 Issue | そもそも起動しない | 起動しないので抽出まで行かない |

ISSUE-38 の手当て (0 件を「親が無い」と即断せず規約外のキー名を報告する) は、Phase E が
走ることを前提にしている。走らない経路はそちらでは塞がらない。

## タスク

- [x] 同梱経路のどこで Phase E を起動するかを決める (同梱節へ D.5 相当を足すか、gate の
      Phase 3 が同梱を適用した直後に続けるか)
  - 同梱節の側へ足した。gate は変更していない。gate SKILL.md 115 行が「Phase D を直接
    名指ししないのは、同梱経路の射程が Phase D 全体とは一致せず、狭める側の canonical が
    同梱節だから」と明記しており、射程を持たない委譲が正しく効いている
  - D.1〜D.4 を並べた先頭の箇条書きには足していない。D.5 はコミットへ入る変更を作らない
    ので、そこへ足すと宣言が実態より広くなる
- [x] 親子 Issue の fixture で、同梱経路から子を全て closed にしたときに親への提案が
      出ることを確認する
  - Phase E の E.1 / E.2 スニペットを SKILL.md から抜き出して親子 fixture へ流した。
    子が全て closed のとき提案側へ倒れ、対照 (子が 1 件 active) では倒れない
  - 恒久テストにはしていない。Phase E の散文は ISSUE-38 が触る予定で、同じフェンスに
    実行 pin を今置くと編集が衝突する
- [x] 起動忘れが静かに続く形を塞げるかを判断し、塞げないなら限界としてどこへ書くかを決める
  - 塞げない。不変条件 C は結果の状態しか見ないので、D.5 の実行忘れそのものは検出しない。
    親子 Issue が実在して子が全て closed になって初めて赤くなる
  - 限界は `scripts/check-issue-closure.py` の docstring へ書いた。同梱節の記述が失われる
    形は `BundledCloseNamesThePropagationStep` が pin する (「D.5 が在るか」ではなく実行を
    指示する一文が在るか。前者では足りないことを変異注入で実測した)

## 関連

- ISSUE-41: クローズ同梱の判定を促す入口。同梱を既定にした側で、spec がこの件を
  「新規 Issue へ切り出す」と決めている
- ISSUE-38: Phase E の親伝播が frontmatter の乖離で静かに発火しない。症状は同じで機序が別。
  手当てが Phase E の起動を前提にしている点が本 Issue と噛み合わない。本 Issue が足した
  不変条件 C は、片側だけキーが乖離した形を「親子リンクが片側にしか無い」で赤くするので、
  ISSUE-38 の穴を外側から狭める。両側が同時に乖離した形は辺が 1 本も立たず 0 組で静かに
  緑になり、そこが残余として ISSUE-38 の射程に残る (両向きとも fixture で実測)
- ISSUE-32: in-repo Issue の検査を配布先で走る状態にする。同梱が既定になる範囲がどこまで
  広がるかは層 2 の配線に依る。不変条件 C も A と同じ理由でそのまま配れない。C を違反に
  すると post-merge クローズ経路が子 close と親 close の間で必ず違反状態を通り、Phase E.4 の
  「提案を拒否して何もしない」が恒久的な赤になる
- ISSUE-45: 不変条件 A が親 Issue に対して Phase E と正反対の要求を出す。本 Issue の調査中に
  見つけた別の欠陥で、本 Issue では触っていない。不変条件 C は「親の箱は未 / 子は全て closed」
  の側を担当し、ISSUE-45 が扱う「親の箱が全て [x] / 子は active」には触れない
